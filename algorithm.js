const a = 1;

console.log(a);

// Buble Sort

const listArr = [44, 3, 4, 3, 64, 12, 43, 65, 43, 87];

const bubleSort = (array) => {

    const setArr = new Set()

  for (let i = 0; i < array.length - 1; i++) {
    if (array[i] > 50) {
      console.log(array[i]);
    } else {
      console.log(array[i]);
    }
  }
};

bubleSort(listArr);
